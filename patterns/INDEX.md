# Pattern Index

No reusable task patterns have been promoted yet.

Add a pattern only after a real task proves it useful. Each pattern must include its trigger, bounded steps, failure modes and verification commands.
